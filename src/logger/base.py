class BaseLogger:
    # def __init__(self, name: str):
    #     self.name = name

    # def log(self, message: str):
    #     print(f"{self.name}: {message}")

    def __init__(self) -> None:
        self.info = print